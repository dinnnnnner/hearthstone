# 2026-09-14 训练并行数调整

训练服务器恢复后，用三个已上线的最终检查点同时采样，对比每个模型 8、12、16 个模拟器，并在最后复测 8 个模拟器。每组使用相同的 16 个种子，每局执行 256 步，保留混合历史对手及原奖励设置。

| 模型 | 8 并行，动作/秒 | 16 并行，动作/秒 | 采样吞吐提升 |
|---|---:|---:|---:|
| 64 层 | 57.26 | 70.95 | 23.9% |
| 256 层 | 48.67 | 62.02 | 27.4% |
| 1024 层 | 25.01 | 34.50 | 37.9% |

基线取首尾两次测试的平均值。12 并行收益较小；每轮固定 16 局时，最后剩余 4 局会降低批量。16 并行的平均推理批量约为 2.5，原来约为 1.8～1.9。

所有配置、所有种子的动作回放均与基线一致，检查点文件哈希在测试前后保持一致。16 并行时，定时采集的三个进程总显存占用最高约 3.6 GB，未发生内存不足。

本次只测量采样：三个模型同时开始，各自完成后退出；不是三个模型持续满负载训练的测量。所有对局在 256 步截断，未执行 PPO 更新，因此上述百分比不能直接当作完整训练提速或棋力提升。正式续训仍需核对中后期吞吐和 PPO 更新时的峰值内存。

服务器协调器 `/root/tavern-human-current-06cd2177/population_resume.py` 的 `--workers` 默认值已改为 16。旧代码默认值为 4，上一轮启动命令显式指定的是 8。下一轮启动应显式传入 `--workers 16`，避免复用旧命令覆盖新默认值。

下一轮参数记录位于 `/root/tavern-four-hour-20260914/next-training.json`。每轮 16 局、每两轮交换对手、外部对手占比 0.4、第一名额外奖励 1 和每枚剩余铸币惩罚 0.01 均保留。训练仍处于到时停止状态，本次未启动续训，也未替换线上模型。

测试脚本、完整结果及协调器备份保存在 `/root/tavern-concurrency-20260914`。协调器修改后通过语法检查及 `--help` 启动检查。配置、代码哈希及逐组测量见同名 JSON 文件。
