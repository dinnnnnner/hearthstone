# 2026-09-15 收尾冻结规则与续训

源码提交 `870ae4f` 将人机手动冻结改为招募回合的收尾选择。商店必须存在当前金币买不起的牌才允许冻结，使用实际费用判断。模型仍可直接结束，不会因为买不起而自动冻结。手动冻结或取消冻结后只能结束招募；换位仍是每回合最多 6 次，禁止立即撤销。真人操作不受这些限制。

模型全局实体增加 `freezeClosing` 与买不起的商店槽位 `freezeOffers`，网络参数和动作编码不变。详细规则见 [动作限制](ai-action-limits.md)。

## 上线与验证

公网运行目录为 `/opt/bobs-tavern/releases/20260915-freeze-close`，三个搜索服务的 release 为 `20260915-freeze-close`。搜索协议为 `own-recruit-freeze-close-v3`。切换前确认没有活跃房间，停止服务后备份状态；保留旧 release，可同时回滚公网和三个搜索服务。

六个在线模型沿用原来的权重。本次只上线动作规则和对应搜索协议。浏览器已逐一验证模型选择、重连、完成首回合战斗和好友房共享模型选择。三个搜索模型通过公开局面合法动作、记忆隔离和零预算直接推理验证。

本地规则 141 项、RL 14 项、服务端 59 项、搜索 TypeScript 7 项和 Python 10 项检查通过。Python 完整测试共 143 项，本机跳过 5 项 CUDA 测试；Blackwell 上通过 CUDA 检查，4 项视频测试因缺少 ffmpeg 跳过。首次完整包测试发现缺失 `scripts/enable-sampling-graphs.py`，已将辅助脚本纳入打包并通过相关 6 项复测。

## 训练

新运行目录及完整检查点在 `/root/autodl-tmp/tavern-freeze-close-20260915`。旧运行目录 `/root/autodl-tmp/tavern-action-limits-20260915` 保留。原有运维入口的运行目录和 population 符号链接已切到新目录。

旧训练先正常停止，再从最近保存的完整检查点迁移。工具只接受核对过的 v1 到 v2 哈希组合，验证动作、实体和历史对手兼容，并核对保留状态摘要。权重、优化器、随机状态、历史池、累计局数和迭代次数不变；旧规则尚未完成的采样不进入新训练。

| 深度 | 迁移时累计局数 | PPO 迭代 |
| --- | ---: | ---: |
| 64 | 3592 | 184 |
| 256 | 3100 | 157 |
| 1024 | 2340 | 102 |

先在独立 smoke 目录从 64 层检查点完成 2 局和一次 PPO，验证 2325 个合法操作、有限训练指标和保存结果。逐步重放两局，113 次冻结均满足资格，之后该席位只能结束回合；录像动作总数与训练统计一致。这份验证权重未用于正式训练。

正式续训在北京时间 15:07:37 启动，沿用原定 15:50:39 截止时间。三种模型仍交换历史对手，每种模型 64 个模拟器、8 个采样进程，每轮 PPO 64 局，每两轮交换。保留 MPS、采样与训练 CUDA Graph、融合 Adam、序列批量 32 和各自学习率。奖励第一名 +2、第八名 −1，无剩金币扣分。

北京时间 15:15:23 已确认三种模型首批各 64 局更新全部保存。累计局数分别为 3656、3164、2404，PPO 迭代分别为 185、158、103；训练指标有限，学习策略动作数与 PPO 样本数一致，采样和训练 CUDA Graph 均启用。15:12 的自动检查健康、无 OOM。

训练日志 `/root/tavern-ops/20260915-freeze-close/population.log`，每 300 秒检查的结果在同目录 `health/latest.json`。新增 `action_counts` 和 `learner_action_counts`，分别统计全局及学习策略的动作。

规则能直接阻止冻结循环，但不能据此认定棋力提高。是否该升本、买牌或刷新仍由 PPO 学习，本次没有加入金币搜索的蒸馏标签。部署、检查点迁移和训练指标见 [机器记录](ai-freeze-close-deployment-20260915.json)。
