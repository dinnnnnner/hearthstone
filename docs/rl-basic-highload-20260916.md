已接入巡检并恢复三路持续训练，选用每路 24 个并行环境、4 个常驻采样进程、每轮 24 局、PPO 序列批量 64。三路共 72 个并行环境。持续任务没有 15 分钟停止时限。

本次从三份已经接入场面、经济反馈的检查点续训，保留优化器、对手池和随机种子游标。开始时 64／256／1024 层分别累计新增 56／64／72 局，原始旧权重及本次调参前的检查点另行保留。

原 15 分钟试训的有效记录中，GPU 平均利用率约 74.9%，CPU 平均使用 14.18／25 核，容器内存平均 65.50／120 GiB。该平均值排除了最初四个资源样本，仍包含采样、更新和保存阶段，不能当作固定工况测量。

调参先测试每路 16 个并行环境、4 个常驻采样进程、每轮 16 局。三路合计 48 个并行环境。PPO 序列批量暂时保留 32，学习率和反馈目标不变。这一档完成后，又测试每路 24 个并行环境、每轮 24 局、PPO 序列批量 64。两档都正常完成，最终选用 24 并发。

临时采样结果通过 `TAVERN_ROLLOUT_SCRATCH` 放到每个任务专用的 `/dev/shm` 目录。不可变策略快照仍在检查点所在磁盘建立硬链接，避免跨文件系统硬链接失败，也不向内存盘复制大模型文件。完成或异常退出时清理本轮临时文件。持久检查点仍写入数据盘。

巡检与训练进程由 [supervise-basic-training.py](../scripts/supervise-basic-training.py) 共同管理。每 10 秒采集资源，每 60 秒保存完整巡检，检查真实采样进度文件和检查点时间，不把不断打印日志当作训练推进。记录保存在任务目录的 `status.json`、`health.json`、`resources.jsonl`、`checks.jsonl` 和异常时的 `alerts.jsonl`。巡检不发送外部消息，也不替换网页模型。

超过 88% 容器内存记为警告，超过 94%、出现新增 OOM、数据盘不足 1 GiB、内存盘不足 2 GiB、900 秒无采样或检查点进度、训练异常或非有限损失时停止任务。停止先请求训练进程退出，随后只清理这次任务的后代进程，并校验 PID 出生时间。已完成批次的检查点保留；未完成批次不承诺保存。任务持有输出根目录的排他锁，避免重复启动。

日志按单个文件 16 MiB 轮换，保留一份上一段记录。每路训练日志仍是本次启动独立文件。巡检不会无限重试异常任务。

服务器操作目录为 `/root/tavern-ops/20260916-basic-highload`，训练输出为 `/root/autodl-tmp/tavern-basic-feedback-old-20260916/depth-{64,256,1024}`。调参前的备份在操作目录的 `before/`。启动命令使用独立任务目录中的 `job.json`，由任务文件指定并发和迭代次数；长期任务没有原 15 分钟试训时限。

本次本地通过 17 项采样、常驻进程、反馈测试和 2 项巡检测试，远端同样通过 19 项。内存盘测试覆盖模型替换后仍读取原策略快照，以及异常后的临时文件清理。

实测结果如下，速度包含采样、参数更新和检查点保存。

| 模型深度 | 原 8 环境，动作／秒 | 16 环境，动作／秒 | 24 环境，动作／秒 |
| --- | ---: | ---: | ---: |
| 64 | 104.31 | 145.44 | 146.79 |
| 256 | 101.40 | 137.61 | 143.32 |
| 1024 | 81.87 | 111.39 | 119.24 |

16 环境每路测试 3 轮、48 局；24 环境每路测试 2 轮、48 局。调参共完成 288 局。24 环境相对原记录的动作吞吐高约 41%～46%，相对 16 环境高约 1%～7%。种子、权重和对手池随训练推进变化，本表不是固定模型、相同种子的严格 A/B，也不是棋力评估。

排除前 60 秒启动阶段及第一路训练结束后的尾段，16 环境的 39 个资源样本中，CPU 平均使用配额的 78.56%，GPU 平均利用率 69.28%；24 环境的 39 个样本中，CPU 平均 86.36%，GPU 平均 60.00%。24 环境的 CPU 在采样阶段持续接近 100%，GPU 峰值为 100%，但没有实现持续满载。GPU 利用率均值没有提高；选择这一档的依据是三路的有效训练吞吐略高。

24 环境的内存峰值为 72.01／120 GiB，显存峰值为 48,679 MiB，约 47.54 GiB。增加并发已碰到 CPU 配额限制；继续占满内存或显存不能据此推断会更快。内存盘承担的是临时轨迹交换，既不预填充剩余内存，也不存放持久模型。

北京时间 2026-09-16 19:11:26 启动持续任务，协调进程 PID 为 225560，三路训练 PID 为 225561、225562、225563。任务文件在 `/root/tavern-ops/20260916-basic-highload/continuous/job.json`，状态和巡检文件在同一目录。该任务从迭代 12／13／14、累计新增 152／160／168 局的检查点恢复，优化器、反馈规则和对手池继续沿用。

19:12 的复查确认三路身份正确、GPU 采样正在推进、没有巡检告警。完整的启动配置、测试输出、每轮指标、资源汇总和代码 SHA-256 见 [机器可读记录](rl-basic-highload-20260916.json)。

19:43 已另外恢复原 QQ 邮件通知，正常状态每 5 分钟一封，新增异常和结束立即通知。详见 [邮件恢复记录](rl-basic-training-email-20260916.json)。
