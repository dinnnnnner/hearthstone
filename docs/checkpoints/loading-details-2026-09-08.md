# 加载画面与棋盘细节

版本标签 `loading-details-2026-09-08`，前端发布 `20260908T073445Z`。上一版 `room-fixes-2026-09-08` 与更早的 Demo 标签保留。

页面开始下载时显示酒馆纹章、旋转金环与提示，入口脚本失败后提供重试。进入棋盘时显示当前素材的实际准备数量，同时最多请求三张图片；三秒后可先进入，八秒后自动结束等待。成功准备的图片在当前页面访问中缓存，图片失败也会推进准备进度。加载期间暂停战斗播放，恢复游客身份时使用同套画面。

木板边缘与皮革底色补充渐变和阴影，增加黄铜铆钉与中央刻纹；卡框、手牌、状态数值和操作按钮调整层次与反馈。新效果由 CSS 与 SVG 绘制，适配手机横竖屏并遵守减少动态效果设置。

本次通过 26 项不同的本地浏览器场景（原有 22 项与新增 4 项加载检查）、11 项公网检查及生产构建。公网检查覆盖脚本失败重试、加载并发上限、手动跳过、八秒自动进入、游客身份、两人/八人好友房、手机布局、主页入口和 SECTOR WebSocket。八人房测试耗时 12.9 秒。引擎与后端未改动，上版的 46 项规则测试和 16 项服务测试本轮未重跑。

仅切换前端目录并重载 Nginx，后端仍使用 `/opt/bobs-tavern/releases/20260908T062430Z`。酒馆 PID 保持 108082，SECTOR PID 保持 108431；五个 HTTPS 健康检查及发布文件哈希验证通过。HTML / CSS / JavaScript 的 gzip 体积约为 3.2 / 29.2 / 222.5 KB，没有新增卡面素材。

恢复代码可执行：

```bash
git worktree add --detach ../hearthstone-loading-details loading-details-2026-09-08
```

部署记录见 [deployment.json](../deployment.json)，回滚步骤见 [部署说明](../../deploy/README.md)。线上身份与房间数据保存在独立目录，不包含在 Git 标签中。
