# 录像采样音效上线

最终发布：`20260910T073352Z`，地址 https://8.153.150.101/tavern/ 。

13 种实机采样覆盖拿牌、购买、出牌、出售、刷新、冻结、升级、发现、开战、招募回合、攻击命中、死亡和三连。其余音效保留合成版，下载失败或尚未加载时也使用合成回退。音量和静音持久保存，暂停、跳过及切后台会停止音源。原视频和处理参数见 [采样记录](../audio-sampling.md)。

发布包按 `/tavern/` 构建，大厅和 Nginx 配置沿用 `20260910T055037Z`。首次发布目录继承了临时目录的 0700 权限，激活检查遇到 403 后自动回滚。随后修正为 0755，并在切换前用 Nginx 用户验证可读性。上传说明已补充权限检查。

公网首轮检查发现音效预加载与卡面争用带宽，导致三个首次加载检查超过 5 秒。最终版本将采样预加载延后至棋盘就绪，并使用低网络优先级。最终版本这些加载检查均通过。

本地生产包验证覆盖 13 个不同用例，预加载调整后重复了其中 7 项。最终公网主运行通过 13 项；完整音效库的检查曾超过默认 5 秒，按可选低优先级下载调整为 15 秒后单独重跑通过。共 14 个公网用例完成验证，包含真实采样解码与播放、音量设置、桌面与触屏拖拽、战斗同步、烧绳、大厅导航和 SECTOR WebSocket。

公网 HTML、JS 与本地生产构建一致。13 个音效均返回 HTTP 200，逐个通过 SHA-256 校验。最终脚本为 `/tavern/assets/index-DATwvPIj.js`。SECTOR PID 保持 117252，酒馆后端 PID 保持 116826。

直接回滚到前一发布：

```bash
ssh root@100.121.69.44 'python3 - /var/www/playroom/releases/20260910T073352Z' < scripts/rollback-site.py
```

该命令恢复 `20260910T073128Z`，其中已经包含音效，但没有延后预加载的调整。若需要恢复本次工作之前的 `20260910T055037Z`，完成上述回滚后再对 `20260910T073128Z` 执行相同回滚脚本。

## 播放问题后续修复

2026-09-10 用户反馈音效不对。发布 `20260910T074830Z` 修复单条下载阻塞整套解码、采样未就绪时切成合成音、采样随机变调三个播放问题。只更换首页入口文件和 JS；13 段 WAV、大厅、后端和 Nginx 配置沿用上一发布。

本地 5 项浏览器测试通过，包括人为卡住一条音效下载和全部素材返回 404。公网 2 项检查通过，覆盖采样解码、购买/冻结实际播放、音量静音保存。上述检查不能验证声音选取是否正确。用户的音色反馈仍未解决，需进一步确认具体操作并逐项试听；不能将播放修复标为音效还原完成。

最新 JS：`/tavern/assets/index-DaR7hQuJ.js`。发布激活通过全部文件哈希及 HTTPS 健康检查。回滚本次播放修复：

```bash
ssh root@100.121.69.44 'python3 - /var/www/playroom/releases/20260910T074830Z' < scripts/rollback-site.py
```
