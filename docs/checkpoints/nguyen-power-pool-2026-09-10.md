# 阮大师候选技能过滤

发布 `20260910T093039Z`，前端与房间服务共用此规则。

按用户要求，为阮大师的一回合技能单独过滤候选。芬利、吉恩和身份揭晓继续使用原有候选规则。此实现结合已查证公告与项目技能的实际触发条件，不代表完整复刻官方 36.4.2 服务端名单或权重。概率仍为均匀打乱。

## 规则依据

官方历史公告明确移除了艾德温、凯瑞尔、弗洛格尔，并限制米尔菲丝四本前、典狱长一本时不能提供。来源与当前版本查证边界见[研究记录](../nguyen-power-pool-research-2026-09-10.md)。这些记录不能单独证明所有技能在当前官方版本中的状态。

其余为本项目按用户要求制定的适配规则。开局专用、跨回合准备或当前临时获取逻辑不能支持的技能排除 `curator`、`nzoth`、`afk`、`cookie`、`eudora`、`cthun`、`ragnaros`、`aranna`、`marin`、`buttons`。原有 `finley`、`nguyen`、`genn`、`patchwerk` 排除继续生效。

按项目技能文本和执行条件，阿莱克丝塔萨四本起提供；沙德沃克、希尔瓦娜斯、学者阿卡扎曼扎拉克、尤格萨隆第三回合起提供；德雷克塔尔、范达尔第七回合起提供。沃恩只在三的倍数回合提供，萨维斯只在四的倍数回合提供。雷诺、泽里克、克拉格次数耗尽后不再提供，了不起的杰弗里斯使用三次后不再提供，蛇眼冷却未结束时不提供。

当回合可以产生效果的技能继续保留，包括雷诺、泽里克、凯尔萨斯、库尔特鲁斯、加里维克斯、塞纳留斯、玛维。效果延续到后续回合本身不构成排除理由，也不把所有有计数器的技能一律排除。种族限制和不重复当前技能的约束继续生效。

## 触发时机

阮大师选定技能后立即执行该技能的回合开始效果。下一回合先提供新选项，避免旧技能在被替换前再次执行。新增瓦丝琪回归用例验证这两个时点。已生成的旧存档选项保留，新一轮生成选项使用过滤规则。

## 验证与发布

105 项引擎/状态测试、29 项服务端测试通过。浏览器本地通过 4 项，包括新增阮大师流程以及原有芬利、吉恩和身份揭晓流程。公网通过 1 项阮大师流程，覆盖立即触发、下一回合候选过滤、刷新保留选项和再次选定技能。生产构建与 `git diff --check` 通过。

发布目录全部文件哈希、五项 HTTPS 健康检查、公网 HTML/JS 与本地构建一致性检查通过。前端 JS `/tavern/assets/index-Du8R37yP.js`。后端 SHA-256 `448d301dcc2c3d5023d75ba90cacd6dc8e1d7b9a4725af00fd7b964e95802a48`。酒馆服务 PID 119033，SECTOR PID 118260 未变。沿用线上大厅及配置。

前端目录 `/var/www/playroom/releases/20260910T093039Z`，后端目录 `/opt/bobs-tavern/releases/20260910T093039Z`。前后端上一版本均为 `20260910T091150Z`，发布目录保存了回滚配置。
