# 烧绳与训练模式

保存标签：`recruit-modes-2026-09-10`。

大厅在英雄选择下方提供对局模式选择，用于人机匹配和新建好友房。浏览器记住上次选择；加入好友房遵循该房间模式。开局后模式固定，对局画面与房间大厅显示当前模式。

- 烧绳模式：招募 90 秒，最后 20 秒出现烧绳，到时自动开战；沿用现有战斗回看时间。
- 训练模式：招募与战斗回看均无倒计时，玩家手动结束招募、返回酒馆；好友房等全部存活玩家确认。

服务器保存 `Room.mode`，训练房间的 `deadline` 为 0，服务器 tick 不自动推进。重启恢复该模式；旧存档和未传模式的客户端默认烧绳模式。非法模式在创建房间前拒绝。训练对局有人在线时不会因两小时没有操作被回收；所有玩家离线五分钟后仍按原规则回收。

本地练习继续不限时。本次不包含强化学习训练接口，游戏规则覆盖沿用此前卡池版本。

验证：生产构建、25 项服务端测试、11 项本地浏览器检查通过。服务端测试覆盖双人手动同步、超时不推进、恢复、旧存档迁移、非法 API 输入和离线回收。公网验证记录见 [deployment.json](../deployment.json)，包括真实训练人机房、训练好友房及原有八人联机。

前后端发布目录分别为 `/var/www/playroom/releases/20260910T053244Z` 与 `/opt/bobs-tavern/releases/20260910T053244Z`。上一前端为 `20260910T052522Z`，上一后端为 `20260910T035242Z`。已备份服务器状态至 `/var/lib/bobs-tavern/state-before-modes-20260910T053244Z.json`，发布后全部文件哈希校验通过。

酒馆服务 PID 更新为 116560，SECTOR PID 110058 保持不变。线上主页与上一前端逐文件一致。
