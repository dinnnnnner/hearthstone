# 酒馆操作原始素材接入

前端发布 `20260910T085411Z`，https://8.153.150.101/tavern/ 。[线上试听](https://8.153.150.101/tavern/audio/listen.html)可单独播放全部变体。

购买、刷新、三连、冻结、解冻、出售六项操作替换为客户端配置引用的素材。购买、刷新和三连按网页动画组合分层，原始片段及处理参数记录在 [配方](../battlegrounds-shop-audio-active.json)。冻结、三个解冻变体、出售五个 WAV 与原始提取文件逐字节相同，播放增益用于平衡响度。移除六个被替换的旧通用音效。

刷新保留五种落桌声、三连保留三种入手声、解冻保留三种碎冰声。每次只选择一个已加载变体，多个变体可用时不立即重复。下载和解码仍独立完成，单个文件失败不阻塞其他声音，也不补播过时操作。

全库 31 项操作共 39 个音频文件。其中 14 项操作使用已找到的客户端对应素材，另外 17 项保留先前的通用炉石素材组合。升级的引用还未确认；已有伙伴、任务、宝藏参考文件未套到无对应触发的操作。查找证据见 [来源说明](../battlegrounds-shop-audio-found.md)。

构建通过；本地 9 项浏览器检查通过，包括逐项音频输出、缺失/卡住的下载、随机变体、阶段结算、鲜血宝石和静音控制。公网六项商店操作流程通过，用时 21.0 秒，解码完整 39 文件；公网静音/音量持久化检查通过。14 个新 WAV、HTML、JS、试听页共 17 个公网哈希与构建一致。测试验证来源与播放功能，未声称听感已得到用户认可。

发布额外打包 WAV 的 gzip，现有 Nginx `gzip_static` 可直接提供。整库解码前 WAV 体积 10,530,636 字节，gzip 合计 7,506,401 字节，实际返回 `Content-Encoding: gzip`。此压缩不改变解压后的音频字节。

沿用线上大厅、配置和后端。发布完整哈希、5 项 HTTPS 健康检查通过。SECTOR PID 118260、酒馆 PID 118137 在此次发布前后均未改变。线上 JS `/tavern/assets/index-C59rGfZx.js`。

回滚本次前端发布：

```bash
ssh root@100.121.69.44 'python3 - /var/www/playroom/releases/20260910T085411Z' < scripts/rollback-site.py
```
