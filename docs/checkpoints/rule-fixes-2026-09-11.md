# 酒馆规则修复部署

发布 `20260911T012613Z`，入口 https://8.153.150.101/tavern/ 。前端和房间服务使用同一份规则。

- `fe03f7c`：查抄宝石可以对酒馆随从使用，只转移目标所在一排相邻随从的宝石。
- `c7787af`：财宝鹦鹉保存跨场战斗的累计伤害，达到 35 后发放点金之触并保存完成状态。
- `643bde9`：冥想不再要求选择随从目标；验证每回合发牌、永久叠加及出售来源随从后保留加成。

构建基线为 `backup/workspace-2026-09-11` 的 `0ba1e5a705c54f233a5e16352b3343c962883326`。先在隔离目录构建基线，确认前端 `index-TQa4Pgbu.js` 与后端 `server.cjs` 的 SHA-256 均与旧线上版本完全相同，再应用以上三个提交。线上已有的音效、界面和规则修改保留，工作区中的其他修改未混入本次提交。

新前端脚本 `/tavern/assets/index-BWfMFyxw.js`。新后端 SHA-256：`b36de91e09b20c2301b297623f4410091598b5442e30a9d2b44b0f9dc35a7c41`。

前端保留原大厅、图片、音频及旧版哈希资源，增量包 579106 字节；后端压缩包 165419 字节。服务配置与 Nginx 配置逐字节不变。切换前校验全部静态文件及后端哈希，激活脚本通过 Nginx 配置及五项 HTTPS 健康检查。公网 HTML、JS 的哈希与本地构建相同。

规则测试 124 项通过；房间服务测试 30 项通过；本地浏览器检查 4 项通过。公网第一轮 8 项通过，涵盖两项规则修复、游客人机、双人好友房同步与重连、手机大厅、两个游戏入口及 SECTOR WebSocket。八浏览器满房检查未通过：第一轮超过 60 秒限制；延长到 180 秒后，英雄技能选择弹窗遮挡结束招募按钮；最后固定无需选技能的英雄、关闭音效并使用训练模式，八人均点击结束招募，但战斗画面未在断言的 5 秒时限内出现。最后一次用时约 2.2 分钟，不能作为满房同步或冷启动性能通过的依据，原因尚未完全定位。线上代码仍只包含上述三项规则修改，复查调整仅在隔离目录中的测试脚本中进行。

SECTOR PID 在部署前后为 `119165`，未重启。酒馆服务从 PID `119777` 切换至 `121905`，自动重启次数为 0。

前端：`/var/www/playroom/releases/20260911T012613Z`。
后端：`/opt/bobs-tavern/releases/20260911T012613Z`。
前一个前后端版本均为 `20260910T105128Z`，保留供回滚。前端回滚信息位于新发布的 `rollback/state.json`，后端回滚信息位于新发布的 `rollback.json`；具体步骤见 `deploy/README.md`。训练服务器与训练产物没有更改。

最终检查酒馆内存为 64,536,576 字节，健康接口正常，自动重启次数为 0。测试房间退出后仍可能在离线宽限期内保留，按既有规则回收。
