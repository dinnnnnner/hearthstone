# 模仿后接入自我对战

`bootstrap_imitation` 将选定的 `best.pt` 转成新的完整 PPO 检查点。它校验真人记录的观察与动作契约，要求候选来自验证最优轮次，并逐张量保留模仿权重。PPO 优化器重新初始化，自我对战局数与迭代数从 0 开始；初始历史池只有该模仿策略的冻结副本，价值塔与价值头重新允许梯度更新。

```sh
PYTHONPATH=rl/python .venv/bin/python -m tavern_rl.bootstrap_imitation \
  --candidate /path/to/deep64/best.pt --output /path/to/bootstrap-64 \
  --learning-rate 3e-5 --hero-pool src/season/ai-hero-pool.json
```

之后用 `tavern_rl.train --resume /path/to/bootstrap-64/latest.pt` 开始完整对局 PPO。首次对手交换要求每个模型先完成实际自我对战，因此三个深度各完成 2 局、一次 PPO，再进入常规 population。名次奖励第一名 +2、第八名 −1，AI 收尾冻结与换位限制继续启用。金币规划保持关闭，先让价值头从真实回报学习；不会用随机价值头构造搜索标签。

## 精度试验

`selective_precision.bf16_tower_linears` 是显式实验上下文，只将策略塔和价值塔的线性矩阵计算切到 BF16，输出立即转回 FP32。主权重、优化器状态、归一化、残差累加、实体与注意力编码、GRU 记忆、动作头和价值输出、概率与对数概率、优势、回报、PPO 损失和梯度裁剪均为 FP32。上下文不会修改权重文件格式或默认训练精度。

精度上下文先安装，CUDA Graph 在其中捕获，并在上下文退出前释放。关闭 autocast 权重缓存，避免优化器更新后重用旧的低精度权重。CPU 回退为 FP32。

```sh
PYTHONPATH=rl/python .venv/bin/python scripts/benchmark-selective-precision.py \
  --candidates-root /path/to/fresh-imitation --dataset /path/to/selected-games \
  --output /path/to/precision.json
```

脚本使用真实公开观察，测量批量 8、32 的完整推理和批量 512 的塔前向、反向与 Adam 耗时，报告概率差、KL 和价值误差。塔基准只验证计算与有限梯度，不代表完整 PPO 或棋力。正式接入前另用相同的完整对局轨迹、初始化权重与随机状态比较 FP32 和 BF16 的完整 PPO 更新，只保留 FP32 试验产生的训练检查点。

采样测试让三个深度同时运行，比较每个模型 64 个模拟器配 4 或 8 个采样进程。固定每模型 64 个种子，每局前 128 次操作，截断轨迹全部丢弃。该测试包含进程启动与图捕获，测的是短局面吞吐，不能直接当作完整训练加速。若 4 进程相对 8 进程超过 5% 吞吐增益，则先采用 4 进程，并继续通过正式训练巡检观察。

## 当前运行

2026-09-15 北京时间 17:37 已启动三个深度的混合自我对战，仍于 19:57:55 截止。64 层使用模仿第 9 轮，256、1024 层使用第 7 轮最佳候选。每个模型先完成 2 局及一次实际 PPO，确认价值参数已更新后接入 population。

正式配置使用 FP32、每模型 64 个模拟器和 4 个采样进程、每轮 64 局，每两轮交换对手。4 进程短测总吞吐为 925.7 次操作/秒，8 进程为 827.6 次操作/秒，约快 12%。这只是短前缀采样结果。BF16 保留为实验工具；64 层交换顺序复测后 FP32 为 1.60 秒，BF16 为 1.67 秒，首次运行开销会夸大 BF16 的收益。用户随后要求停止继续测试并直接训练，正式任务不启用 BF16。

运行目录为 `/root/autodl-tmp/tavern-fresh-selfplay-20260915/population`，任务记录和五分钟检查为 `/root/tavern-ops/20260915-fresh-selfplay/`。见[接入与性能记录](rl-fresh-selfplay-validation-20260915.json)。

## 特征准备优化

17:44 后切换到 `/root/autodl-tmp/tavern-packed-features-20260915/population`，任务与五分钟巡检为 `/root/tavern-ops/20260915-packed-features/`。三个模型均从实际累计 66 局继续，保留完整优化器和历史池。截止时间、奖励和 FP32 精度保持原配置。

实时采样中 CPU 的 25 核配额已满，平均推理批量约 3.6。`pack_entities` 将 9 组 int64 索引合并传输，再按原形状建立视图；数值字段单独保留 FP32。每次打包的主要数据传输从 10 次减少到 2 次。模型缓存静态卡牌定义的解析结果，动态牌面仍逐次读取；缓存不包含依赖模型权重的嵌入，参数更新不会使用旧嵌入。缓存仅适用于该模型固定的 schema。

CPU 与 CUDA 上检查整数、FP32 字段和负零的字节均与原传输方式相同，循环记忆回归通过。17:45 已确认三个训练进程恢复。这里没有声称新的完整训练加速倍率，后续吞吐由正常训练日志记录。

## 2026-09-16 上线与续训

13 小时任务于北京时间 06:52 正常停止。64、256、1024 层分别保存 7554、7618、7298 局，PPO 更新次数为 119、120、115，结束巡检未发现异常。

08:23 前完成普通版和搜索版六个服务的权重更新，发布目录为 `20260915T235413-publish-4h-models`。推理检查和客户端模型选择、重连、实际出牌及好友房验证通过，六个服务均有实际推理记录，未发生错误或降级。部署验证记录位于本机 `~/.local/share/tavern-ops/jobs/20260915T235413-publish-4h/model-verification.json`。

三个模型于北京时间 08:23:05 从上述完整检查点续训 4 小时，计划 12:23:05 停止。保留 FP32、每模型 64 个模拟器、4 个采样进程、每轮 64 局、MPS、融合 Adam 和训练 CUDA Graphs。金币规划训练保持关闭。运行目录仍为 `/root/autodl-tmp/tavern-packed-features-20260915/population`，本次任务记录位于 `/root/tavern-ops/20260915T235413-publish-4h/`，五分钟巡检与邮件转发均绑定本次任务。

## 2026-09-16 直接搜索续训

实例关机后，原训练进程退出。10:03:55 从 64、256、1024 层累计 7938、8066、7746 局的完整检查点恢复，截止时间仍为 12:23:05。新代码位于 `/root/autodl-tmp/tavern-gold-sales-20260916`，沿用原人口目录，保留优化器、随机状态和历史对手池。起始检查点备份位于 `/root/tavern-ops/20260916-gold-sales/recovery-checkpoints`。

本轮开启 `gold_planning.direct=true`。搜索直接选择学习席位的实际动作，策略学习方案价值生成的目标分布，价值头学习最终名次，搜索轨迹不再作为普通 PPO 样本更新。确定性步骤只算一次，首次随机事件展开 12 条路线，所有合法卖牌及商店购买动作均进入比较。新回合、刷新以及其他经济操作后按真实局面重新搜索。跨回合经济收入与回本时间的显式预测尚未加入。

直接搜索开销高于原策略采样，为缩短每次更新的等待，每个模型改为每批 8 局、8 个模拟器、4 个采样进程；保留 FP32、MPS、融合 Adam、序列批量 32 和 CUDA Graph。任务、五分钟巡检和邮件通知位于 `/root/tavern-ops/20260916-gold-sales/`。

单局 GPU 功能验证完成了 29 次搜索决策和 2 次优化器更新，包含实际卖牌、刷新和升本；该局学习席位排名第八，仅证明采样、名次回报和优化器能完整运行，不构成棋力提升证据。10:06 检查时，三个正式模型分别记录 484、411、271 次搜索决策，无搜索回退，尚未完成本轮首次参数更新。在线模型仍为此前发布版本。

## 2026-09-16 持续对局与卡牌经济资源训练

11:25:25 切换到 `/root/autodl-tmp/tavern-streaming-20260916`，三个模型从7946、8074、7762局继续，保留参数、优化器和历史池。旧任务停止后将完整检查点备份到 `/root/tavern-ops/20260916-streaming/recovery-checkpoints`，原人口目录继续使用。截止时间仍为12:23:05。

采用 [持续对局训练](rl-training.md)：默认每两个回合更新PPO，原对局继续进行；加入战斗和卡牌经济资源预测，同局面分叉先比较4次，按结果追加至最多12次。分支默认推演2回合，真实名次来自持续运行的完整对局。按用户要求启用第8回合存活升本奖励，4本0.05、5本及以上0.075，一局只给一次。原每动作直接搜索和独立阶段奖励关闭。保留FP32、MPS、融合Adam、CUDA Graphs，每模型8局、8个模拟器、4个采样进程。

11:27:59 已确认三个正式模型分别保存第127、129、125轮更新，每个模型本轮完成2次PPO优化及1次分叉辅助更新；各自8局仍在进行，辅助损失有限，分支无截断。三个深度的CUDA功能验证通过；本地30项相关测试和服务器16项专项测试通过，双进程完整对局验证记录到了4本及5本的实际奖励且没有重复发放。这些结果证明流程可运行，不代表棋力已经提升。

任务、巡检与邮件目录为 `/root/tavern-ops/20260916-streaming/`。首封邮件于11:25:28获SMTP接受。训练仍继承每席每回合64次总操作上限，冻结1次、换位6次；后期经济循环可能受到总操作上限影响，尚未移除。网页模型尚未发布本次训练结果。接入记录见 [验证记录](rl-streaming-validation-20260916.json)。

## 2026-09-16 逐牌金币估值续训

11:53:11 切换到 `/root/autodl-tmp/tavern-card-value-20260916`。64、256、1024层分别从实际保存的7947、8077、7762局，第134、136、131轮继续，截止时间仍为12:23:05。旧权重、优化器和历史对手池保留，完整备份位于 `/root/tavern-ops/20260916-card-value/recovery-checkpoints`。

启用 [逐牌金币估值](rl-card-value.md)，版本为 `card-cash-v1`。估值读取当前阵容与每张候选牌，铜须在自己场上时，酒馆战吼牌的后续推演会包含相应配合。估值通过从零初始化的投影进入动作类型和卡牌选择；策略仍由PPO学习，没有改成单纯按卡牌价格取最大值。每个采样进程抽取25%的分叉根局面，每次最多一张随从，比较卡牌与0～12金币的替换结果，受实际金币上限约束。

36项相关测试通过；双进程完整训练及续训共完成4局，覆盖商店、手牌和场上随从。三个深度的GPU验证均完成了非零逐牌辅助更新与严格模型加载。11:56:54确认三个正式任务已保存新架构下的PPO更新，仍各有8局进行中。当时正式任务的已完成前缀尚未产生有效金币标签，不能据此声称已经学会卡牌价格或铜须配合。接入详情见 [验证记录](rl-card-value-validation-20260916.json)。

五分钟巡检与邮件改绑 `/root/tavern-ops/20260916-card-value/`，首封邮件于11:53:14获SMTP接受。普通和搜索版网页服务仍使用此前发布权重。

## 2026-09-16 统一动作估值续训

12:11:14 切换到 `/root/autodl-tmp/tavern-action-value-20260916`，原定截止时间仍为12:23:05。64、256、1024层分别从7947、8077、7762局，第137、140、135轮继续，保留原主干、Adam状态和历史对手池。完整备份位于 `/root/tavern-ops/20260916-action-value/recovery-checkpoints`。

启用 [统一动作估值](rl-action-value.md)，版本 `soft-q-v1`。真实轨迹和同局面分支都训练动作回报 Q，替代此前的 PPO 与分支策略交叉熵；逐牌金币估值继续单独校准。训练按估值分布采样，直接推理取最高估值合法动作。FP32、MPS、融合Adam、CUDA Graphs及两回合持续更新沿用原配置。

39项相关测试、6局完整训练及单进程/双进程续训通过。三个深度的GPU验证均完成非零动作 Q 更新、分支回归和严格检查点加载。新估值尚需训练和对局评估，不能将功能验证当作棋力提升。任务、五分钟巡检与邮件已改绑 `/root/tavern-ops/20260916-action-value/`；网页仍使用此前发布权重。接入详情见 [验证记录](rl-action-value-validation-20260916.json)。

北京时间12:14:00确认三个模型分别保存第138、141、136轮更新，均有非零动作回报损失和实际分支 Q 回归，8局对战仍在持续进行。

## 2026-09-16 再续训四小时

上一轮于12:23:05正常到期。按用户要求，12:24:41从三个最新完整检查点再续训4小时，截止时间为北京时间16:24:41。64、256、1024层分别从7947、8077、7762局，第140、143、137轮继续，保留动作估值架构、优化器和历史池。仍使用 `/root/autodl-tmp/tavern-action-value-20260916`，任务与五分钟巡检、邮件改绑 `/root/tavern-ops/20260916-action-value-4h/`。检查点备份在该目录的 `recovery-checkpoints`。详见 [续训记录](rl-action-value-4h-20260916.json)。

## 2026-09-16 场面战力评估续训

13:23:25 切换到 `/root/autodl-tmp/tavern-scene-value-20260916`，截止时间仍为16:24:41。三个深度分别从7963、8093、7770局，第161、165、155轮继续，保留权重、Adam状态和历史池。旧任务已停止，最新检查点备份位于 `/root/tavern-ops/20260916-scene-value/recovery-checkpoints`。

启用 [场面战力评估](rl-scene-value.md)，版本 `combat-benchmark-v1`。当前场面与动作后的未来场面用同一组最多3个对手、每对手4次直接战斗作监督，包含胜负平、伤害和淘汰风险。场面变化及其与当前指标的交互进入动作Q，由实际回报学习权重，没有增加固定战力奖励。真实对局、两回合分支、逐牌金币估值、资源预测和原有奖励继续使用。

42项Python测试、3项战斗测试、TypeScript检查通过；完整训练及双进程续训完成6局，累计生成16556场基准战斗标签。最终版本三个深度的GPU验证均完成场面头、动作Q和分支监督更新，严格模型重载通过。任务与五分钟巡检、邮件通知已改绑 `/root/tavern-ops/20260916-scene-value/`。功能验证不能代替棋力评估。详见 [接入记录](rl-scene-value-validation-20260916.json)。

北京时间13:26:39确认三个模型分别保存第162、166、156轮更新，均有实际基准战斗标签和非零场面头损失。

## 2026-09-16 多时间尺度续训

13:50:39 切换到 `/root/autodl-tmp/tavern-multi-horizon-20260916`，截止时间仍为16:24:41。三个模型保留原有参数和Adam状态，接入 [1、3、5回合场面与经济预测](rl-multi-horizon.md)。四局真实自我对战预热产生18个回合、361个参照阵容，正式训练继续补充参照池。

47项Python检查、3项战斗测试及三个深度的GPU更新验证通过。13:53:48确认64、256、1024层分别保存第167、171、162轮更新，都包含实际战力和经济监督，首批更新未缺少参照。任务、五分钟巡检和邮件改绑 `/root/tavern-ops/20260916-multi-horizon/`，详见 [验证记录](rl-multi-horizon-validation-20260916.json)。网页权重仍为此前发布版本，棋力提升尚需实战验证。
