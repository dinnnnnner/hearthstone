# 2026-09-15 模型上线与两小时续训

上一轮三遍模仿与八小时自我对战已完成，自我对战于北京时间 06:29:10 停止。三个模型都保留了七把玩家对局的模仿记录。

| 模型 | 本次上线累计局数 | 迭代 |
|---|---:|---:|
| 64 层 | 2568 | 162 |
| 256 层 | 2140 | 136 |
| 1024 层 | 1284 | 81 |

上线权重从已停止的最终检查点导出。计算服务器版本为 `20260915-live`，公网版本为 `20260915-models-live`；旧版本 `20260914-trained`、`20260914-models-trained` 保留用于回滚。客户端保留原模型选择标识，更新累计局数与检查点哈希。现有推理程序和服务代码保持原版本。

三个模型各通过 16 步推理检查，页面模型选择、重连、战斗和好友房验证通过。测试后各模型都有正常推理决策，无推理错误或回退。完整检查点哈希和验证结果见同名 JSON 文件。

上线后从当前完整训练检查点继续自我对战两小时，北京时间 07:42:58 启动，09:42:58 自动停止。三个学习进程均已启动，分别使用 16 个模拟器，每轮 16 局，每两轮交换历史对手。第一名排名奖励 +2、第八名 −1，每枚剩余铸币惩罚 0.01。

本次工作目录为训练服务器 `/root/tavern-live-20260915`，包含 `resume.json`、`population.log` 和 `resume-verification.json`。训练检查点继续写入 `/root/tavern-four-hour-20260914/population/member-{0,1,2}/training/latest.pt`，公网使用本次固定导出版本。
