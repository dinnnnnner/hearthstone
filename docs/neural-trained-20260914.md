# 2026-09-14 自我对战模型上线

将当天 18:45 到时停止的自我对战最终检查点接入公网，替换此前的重复模仿学习版本。

| 模型 | 上线前累计局数 | 上线后累计局数 | 训练迭代 |
|---|---:|---:|---:|
| 64 层 | 1512 | 1768 | 112 |
| 256 层 | 1196 | 1404 | 90 |
| 1024 层 | 628 | 740 | 47 |

三个完整检查点均保留 5 把玩家对局的模仿记录，并确认使用第一名 +2、第八名 −1 的排名奖励，以及每枚剩余铸币 −0.01 的惩罚。累计模仿更新分别为 2613、1375、503 次。

检查点取自训练服务器 `population/member-{0,1,2}/training/latest.pt`，导出时持有运行锁。部署保留计算服务器现有推理程序及公网服务代码，仅替换权重、模型信息和验证记录。模型选择标识保持不变，计算继续由 `100.97.24.15` 执行，经隧道供公网使用。

计算版本为 `20260914-trained`，公网版本为 `20260914-models-trained`。对应上一版本分别为 `20260914-repeat`、`20260914-models-repeat`，可用于回滚。完整检查点哈希、推理检查和线上验证结果见同名 JSON 记录。

上线后训练保持停止。本次验证确认模型能够正常提供对局，未评估相较旧版的胜率变化。
