# 最新两局模仿后继续八小时自我对战

从已上线的 64、256、1024 层最终训练检查点继续，原累计局数为 1768、1404、740。

本次选取请求时最新的两条已结束玩家记录：

| 对局 | 英雄 | 记录操作数 | 名次 |
|---|---|---:|---:|
| `bc6f66de-4c23-4623-8098-ead0f82baa95` | 马林 | 500 | 1 |
| `3748ec6d-1661-4b6d-aee0-5a4a8e9cca46` | 加里维克斯 | 183 | 1 |

两局都有 `capture_gap`、`automatic_end` 标记。保留通过校验的连续片段，在断点重置循环记忆。合计 683 个操作、52 个长度不超过 16 的训练块，每个模型学满三遍，共 156 次更新、2049 次操作监督。学习率为 `1e-5`，价值分支参数冻结。所有样本用于训练，操作一致率不是独立棋力评估。

服务器控制程序已启动，按以下顺序执行：

1. 三个模型分别完成三遍模仿，检查全部样本覆盖、有限权重及样本负对数似然下降。
2. 将模仿权重装回完整 PPO 检查点，验证优化器、随机数状态、训练计数、历史对手及奖励配置保留。安装失败时恢复原检查点和清单。
3. 从实际启动自我对战时开始计时八小时，到时停止。三个模型各 16 个模拟器，每轮 16 局，每两轮交换对手，外部对手占比 0.4。

第一名排名奖励 +2、第八名 −1，每枚剩余铸币惩罚 0.01。模仿耗时不占八小时自我对战时长。本次没有更换线上推理权重。

工作目录为 `/root/autodl-tmp/tavern-two-games-8h-20260914`，`status.json` 记录当前阶段，`control.log` 为流程日志。模仿结束后产生 `selfplay-launch.json`，其中给出实际开始与截止时间；`population.log` 记录自我对战监督进程。

自我对战继续写入 `/root/tavern-four-hour-20260914/population/member-{0,1,2}/training/latest.pt`。原三个完整检查点已按 SHA-256 校验并归档到本机 `/home/zich/.local/share/tavern-training-archives/20260914-pre-two-games`，共 3013809872 字节。安装验证完成后，控制程序才删除训练数据盘上的临时备份。磁盘维护进程已恢复运行。

同名 JSON 是启动时的验证快照，不代表八小时训练已经完成。后续状态以服务器工作目录为准。
