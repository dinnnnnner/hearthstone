[package]
name = "tavern-rules-ffi"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tavern-rules = { path = "../rules" }
serde_json.workspace = true
